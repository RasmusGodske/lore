# Topics

One file per subject, named by a short lowercase slug (`nightly-import.md`, not
`The Nightly Import Job.md`). A subject that accumulates several documents becomes a directory
with its own `index.md`, and individual investigations go in `history/YYYY-MM-DD-<slug>.md`
under it.

Nothing documented yet.
